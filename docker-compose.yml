
services:
  postgres:
    image: postgres:15
    container_name: expenseShare_postgres
    restart: always
    environment:
      POSTGRES_DB: expenseShare_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: jawedhawari123
    ports:
      - "5432:5432"
    volumes:
      # ✅ Your database files will be stored locally in ./db_data folder
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4
    container_name: expenseShare_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@ecommerce.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy


volumes:
  postgres_data: